name: Functions

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Foo
        if: ${{ contains(fromJson('["staging", "feature", "production", "experiment-staging", "experiment-production"]'), github.event.repository.name) }}
        run: echo It works 🎉
